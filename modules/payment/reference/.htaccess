# Reference scripts include local diagnostics and historical migration helpers.
# They must never be callable in a deployed application.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
